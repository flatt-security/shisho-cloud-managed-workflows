query {
  aws {
    accounts {
      s3 {
        buckets {
          metadata {
            id
          }
          encryptionConfiguration {
            rules {
              encryptionByDefault {
                sseAlgorithm
              }
            }
          }

          tags {
            key
            value
          }
        }
      }
    }
  }
}
