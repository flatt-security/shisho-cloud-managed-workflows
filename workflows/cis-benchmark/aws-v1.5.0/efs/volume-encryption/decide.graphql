query {
  aws {
    accounts {
      efs {
        fileSystems {
          metadata {
            id
          }
          name
          encrypted

          tags {
            key
            value
          }
        }
      }
    }
  }
}
