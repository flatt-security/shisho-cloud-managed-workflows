query {
  googleCloud {
    projects {
      id
      network {
        vpcNetworks {
          metadata {
            id
          }
          subnetworks {
            logConfiguration {
              filterExpression
              aggregationInterval
              enable
              flowSampling
              metadata
            }
          }
        }
      }
    }
  }
}
