query {
  aws {
    accounts {
      cloudFront {
        distributions {
          metadata {
            id
          }

          config {
            logging {
              bucketId
            }
          }

          tags {
            key
            value
          }
        }
      }
    }
  }
}
